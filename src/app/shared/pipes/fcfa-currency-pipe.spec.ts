import { FcfaCurrencyPipe } from './fcfa-currency-pipe';

describe('FcfaCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FcfaCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
