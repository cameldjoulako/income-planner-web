import { Routes } from '@angular/router';

import { RetirementPage } from './features/retirement/pages/retirement-page/retirement-page';

export const routes: Routes = [{ path: '', component: RetirementPage }];
